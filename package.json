{
  "name": "root",
  "private": true,
  "scripts": {
    "add": "lerna add",
    "bootstrap": "lerna bootstrap",
    "environment": "lerna run environment --stream",
    "build": "lerna run build --stream --concurrency=4",
    "deploy": "lerna run deploy --stream",
    "deploy:local": "lerna run deploy:local --stream -- -- --colors",
    "lerna": "lerna",
    "lint": "lerna run lint --stream --concurrency=4",
    "postinstall": "npm run bootstrap",
    "remove": "lerna run remove --stream",
    "start": "lerna run start --stream -- -- --colors",
    "test": "lerna run test --stream --concurrency=4"
  },
  "devDependencies": {
    "@babel/core": "^7.13.14",
    "@babel/preset-env": "^7.13.12",
    "@babel/preset-typescript": "^7.13.0",
    "@types/aws-lambda": "^8.10.76",
    "@types/jest": "^26.0.22",
    "@types/node": "^15.3.0",
    "@typescript-eslint/eslint-plugin": "^4.23.0",
    "@typescript-eslint/parser": "^4.23.0",
    "babel-jest": "^26.6.3",
    "eslint": "^7.26.0",
    "eslint-config-prettier": "^8.3.0",
    "jest": "^26.6.3",
    "lerna": "^4.0.0",
    "prettier": "^2.3.0",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.3"
  }
}
